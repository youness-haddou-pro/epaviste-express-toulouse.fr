---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro'; // 👈 ajout
import MobileCTA from '../components/MobileCTA.astro';
import Hero from '../components/Hero.astro';
import Services from '../components/Services.astro';
import Zone from '../components/Zone.astro';
import Tarifs from '../components/Tarifs.astro';
import Process from '../components/Process.astro';
import Reviews from '../components/Reviews.astro';
import LatestPosts from '../components/LatestPosts.astro';
import Contact from '../components/Contact.astro';
import EcoEngagement from '../components/EcoEngagement.astro';
import Footer from '../components/Footer.astro';
import { data } from '../data/siteData';

const title = `Épaviste Toulouse – Enlèvement gratuit en 1–2 h | ${data.name}`;
const description = "Enlèvement d’épaves gratuit à Toulouse et 100 km autour. Intervention en 1–2 h, certificat de destruction (avec ou sans carte grise), zéro frais cachés. Appelez le 07 75 72 60 36.";
const jsonld = {
  '@context': 'https://schema.org',
  '@type': 'AutomotiveBusiness',
  name: data.name,
  telephone: data.phoneCompact,
  email: data.email,
  address: {
    '@type': 'PostalAddress',
    streetAddress: data.address.street,
    postalCode: data.address.postalCode,
    addressLocality: data.address.city,
    addressRegion: data.address.region,
    addressCountry: 'FR',
  },
  areaServed: 'Toulouse et 100 km',
  openingHours: data.hours,
  priceRange: '€',
  url: '/',
  image: '/images/epaviste-hero.png',
  makesOffer: [{
    '@type': 'Offer',
    name: "Enlèvement d'épave gratuit",
    price: 0,
    priceCurrency: 'EUR',
    eligibleRegion: { '@type': 'Place', name: 'Toulouse (100 km)' },
  }],
};
---

<Layout title={title} description={description} ogImage="/images/epaviste-hero.png" jsonld={jsonld} lang="fr">
  <MobileCTA phoneCompact={data.phoneCompact} />
  <Hero data={data} />
  <Services />
  <Zone data={data} />
  <Tarifs />
  <Process phone={data.phone} />
  <Reviews />
  <LatestPosts limit={3} />
  <Contact data={data} />
  <div class="mx-auto max-w-6xl px-6">
    <EcoEngagement className="mt-10" />
  </div>
</Layout>
