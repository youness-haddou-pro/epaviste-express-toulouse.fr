<section id="avis" class="mx-auto max-w-6xl px-6 py-16">
  <header class="mb-8">
    <h2 class="text-2xl font-bold md:text-3xl">Avis clients</h2>
    <p class="mt-2 text-gray-600">Ajoutez ici vos témoignages (objectif : 4.8★ / 5).</p>
  </header>

  <div class="grid gap-6 md:grid-cols-3">
    {[
      { name: "Paul D.", text: "Intervention super rapide, enlèvement gratuit comme annoncé." },
      { name: "Sonia R.", text: "Très pro et papier de destruction reçu, je recommande." },
      { name: "Imad K.", text: "Arrivés en 1 h, équipe ponctuelle, merci !" },
    ].map((r) => (
      <figure class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-gray-200">
        <div class="flex items-center gap-2 text-amber-500" aria-label="5 étoiles">★★★★★</div>
        <blockquote class="mt-3 text-sm text-gray-700">“{r.text}”</blockquote>
        <figcaption class="mt-3 text-xs text-gray-500">{r.name}</figcaption>
      </figure>
    ))}
  </div>
</section>