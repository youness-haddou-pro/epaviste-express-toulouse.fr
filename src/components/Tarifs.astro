---
/* pas de props nécessaires */
---

<section id="tarifs" class="relative mx-auto max-w-6xl px-6 py-16">
  <!-- décor discret en haut -->
  <div class="pointer-events-none absolute inset-x-0 top-0 -z-10 h-28 bg-gradient-to-b from-emerald-100/60 to-transparent opacity-70"></div>

  <header class="mb-10">
    <h2 class="text-2xl font-bold tracking-tight md:text-3xl">Tarifs & conditions</h2>
  </header>

  <div class="grid gap-6 md:grid-cols-2">
    <!-- Carte : Gratuit -->
    <article
      class="price-card reveal group relative overflow-hidden rounded-2xl bg-white p-6 shadow-sm ring-1 ring-gray-200
             transition duration-300 hover:-translate-y-1 hover:shadow-xl"
      style="--delay:0ms; --grad:linear-gradient(120deg, rgba(16,185,129,.55), rgba(16,185,129,.12), rgba(16,185,129,.55));"
    >
      <!-- bordure gradient -->
      <div class="pointer-events-none absolute inset-0 rounded-2xl opacity-0 transition-opacity duration-300 group-hover:opacity-100"
           style="mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
                  -webkit-mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
                  -webkit-mask-composite: xor; mask-composite: exclude; padding:1px; background:var(--grad);"></div>

      <!-- spotlight -->
      <div class="pointer-events-none absolute inset-0 opacity-0 transition-opacity duration-300 group-hover:opacity-100"
           style="background: radial-gradient(380px circle at var(--x,50%) var(--y,50%), rgba(16,185,129,.08), transparent 40%);"></div>

      <!-- badge -->
      <div class="absolute right-4 top-4 rounded-full bg-emerald-600 px-3 py-1 text-xs font-semibold text-white shadow-sm">
        0 € enlèvement
      </div>

      <h3 class="relative z-10 font-semibold">Ce qui est <span class="text-emerald-700">gratuit</span></h3>
      <ul class="relative z-10 mt-3 space-y-2 text-sm text-gray-700">
        <li class="flex items-start gap-2">
          <span class="mt-1 inline-flex h-5 w-5 items-center justify-center rounded-full bg-emerald-100 text-emerald-700 ring-1 ring-emerald-200">
            <!-- check -->
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 20 20"
              class="h-3.5 w-3.5"
              aria-hidden="true"
              focusable="false"
              fill="currentColor"
            >
              <path
                fill-rule="evenodd"
                clip-rule="evenodd"
                d="M16.7 6.3a1 1 0 0 0-1.4-1.4L8 12.2 4.7 8.9A1 1 0 1 0 3.3 10.3l4 4a1 1 0 0 0 1.4 0l8-8z"
              />
            </svg>

          </span>
          <span>Enlèvement d’épave sur Toulouse et 100 km.</span>
        </li>
        <li class="flex items-start gap-2">
          <span class="mt-1 inline-flex h-5 w-5 items-center justify-center rounded-full bg-emerald-100 text-emerald-700 ring-1 ring-emerald-200">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 20 20"
              class="h-3.5 w-3.5"
              aria-hidden="true"
              focusable="false"
              fill="currentColor"
            >
              <path
                fill-rule="evenodd"
                clip-rule="evenodd"
                d="M16.7 6.3a1 1 0 0 0-1.4-1.4L8 12.2 4.7 8.9A1 1 0 1 0 3.3 10.3l4 4a1 1 0 0 0 1.4 0l8-8z"
              />
            </svg>

          </span>
          <span>Déplacement et prise en charge complète.</span>
        </li>
        <li class="flex items-start gap-2">
          <span class="mt-1 inline-flex h-5 w-5 items-center justify-center rounded-full bg-emerald-100 text-emerald-700 ring-1 ring-emerald-200">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 20 20"
              class="h-3.5 w-3.5"
              aria-hidden="true"
              focusable="false"
              fill="currentColor"
            >
              <path
                fill-rule="evenodd"
                clip-rule="evenodd"
                d="M16.7 6.3a1 1 0 0 0-1.4-1.4L8 12.2 4.7 8.9A1 1 0 1 0 3.3 10.3l4 4a1 1 0 0 0 1.4 0l8-8z"
              />
            </svg>
          </span>
          <span>Certificat de destruction inclus.</span>
        </li>
      </ul>
    </article>

    <!-- Carte : Frais éventuels -->
    <article
      class="price-card reveal group relative overflow-hidden rounded-2xl bg-white p-6 shadow-sm ring-1 ring-gray-200
             transition duration-300 hover:-translate-y-1 hover:shadow-xl"
      style="--delay:80ms; --grad:linear-gradient(120deg, rgba(245,158,11,.55), rgba(245,158,11,.12), rgba(245,158,11,.55));"
    >
      <div class="pointer-events-none absolute inset-0 rounded-2xl opacity-0 transition-opacity duration-300 group-hover:opacity-100"
           style="mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
                  -webkit-mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
                  -webkit-mask-composite: xor; mask-composite: exclude; padding:1px; background:var(--grad);"></div>
      <div class="pointer-events-none absolute inset-0 opacity-0 transition-opacity duration-300 group-hover:opacity-100"
           style="background: radial-gradient(380px circle at var(--x,50%) var(--y,50%), rgba(245,158,11,.08), transparent 40%);"></div>

      <h3 class="relative z-10 font-semibold">Frais éventuels</h3>
      <ul class="relative z-10 mt-3 space-y-2 text-sm text-gray-700">
        <li class="flex items-start gap-2">
          <span class="mt-1 inline-flex h-5 w-5 items-center justify-center rounded-full bg-amber-100 text-amber-700 ring-1 ring-amber-200">
            <!-- € -->
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              class="h-3.5 w-3.5"
              aria-hidden="true"
              focusable="false"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path d="M16 6c-1-.7-2.2-1-3.5-1C9.5 5 7 7.5 7 10.5S9.5 16 12.5 16c1.3 0 2.5-.3 3.5-1" />
              <path d="M7 9h6M7 12h6" />
            </svg>
          </span>
          <span>Hors zone (au-delà de 100 km) : <strong>20–100 €</strong> selon distance.</span>
        </li>
        <li class="flex items-start gap-2">
          <span class="mt-1 inline-flex h-5 w-5 items-center justify-center rounded-full bg-amber-100 text-amber-700 ring-1 ring-amber-200">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              class="h-3.5 w-3.5"
              aria-hidden="true"
              focusable="false"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path d="M4 14h5l1-4h6l2 4h2" />
            </svg>
          </span>
          <span>Cas particuliers lourds (poids lourds, machines agricoles) : <em>sur devis</em>.</span>
        </li>
      </ul>
    </article>
  </div>

  <div class="mt-6 grid gap-6 md:grid-cols-2">
    <!-- Carte : Documents requis -->
    <article
      class="price-card reveal group relative overflow-hidden rounded-2xl bg-white p-6 shadow-sm ring-1 ring-gray-200
             transition duration-300 hover:-translate-y-1 hover:shadow-xl"
      style="--delay:160ms; --grad:linear-gradient(120deg, rgba(107,114,128,.45), rgba(107,114,128,.12), rgba(107,114,128,.45));"
    >
      <div class="pointer-events-none absolute inset-0 rounded-2xl opacity-0 transition-opacity duration-300 group-hover:opacity-100"
           style="mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
                  -webkit-mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
                  -webkit-mask-composite: xor; mask-composite: exclude; padding:1px; background:var(--grad);"></div>
      <div class="pointer-events-none absolute inset-0 opacity-0 transition-opacity duration-300 group-hover:opacity-100"
           style="background: radial-gradient(380px circle at var(--x,50%) var(--y,50%), rgba(107,114,128,.08), transparent 40%);"></div>

      <h3 class="relative z-10 font-semibold">Documents requis</h3>
      <ul class="relative z-10 mt-3 space-y-2 text-sm text-gray-700">
        <li class="flex items-start gap-2">
          <span class="mt-1 inline-flex h-5 w-5 items-center justify-center rounded-md bg-gray-100 text-gray-700 ring-1 ring-gray-300">
            <!-- doc -->
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              class="h-3.5 w-3.5"
              aria-hidden="true"
              focusable="false"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path d="M7 4h7l4 4v12H7z" />
              <path d="M14 4v4h4" />
            </svg>
          </span>
          <span>Carte grise (si possible, intervention possible <em>sans</em> également).</span>
        </li>
        <li class="flex items-start gap-2">
          <span class="mt-1 inline-flex h-5 w-5 items-center justify-center rounded-md bg-gray-100 text-gray-700 ring-1 ring-gray-300">
            <!-- ID -->
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              class="h-3.5 w-3.5"
              aria-hidden="true"
              focusable="false"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <rect x="4" y="6" width="16" height="12" rx="2" />
              <circle cx="9" cy="12" r="2" />
              <path d="M13 12h5M13 15h5" />
            </svg>
          </span>
          <span>Pièce d’identité.</span>
        </li>
        <li class="flex items-start gap-2">
          <span class="mt-1 inline-flex h-5 w-5 items-center justify-center rounded-md bg-gray-100 text-gray-700 ring-1 ring-gray-300">
            <!-- key -->
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              class="h-3.5 w-3.5"
              aria-hidden="true"
              focusable="false"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <circle cx="8" cy="10" r="4" />
              <path d="M12 10l8 8m-4-4l-2 2" />
            </svg>
          </span>
          <span>Clés du véhicule.</span>
        </li>
      </ul>
    </article>

    <!-- Carte : Délais & engagements -->
    <article
      class="price-card reveal group relative overflow-hidden rounded-2xl bg-white p-6 shadow-sm ring-1 ring-gray-200
             transition duration-300 hover:-translate-y-1 hover:shadow-xl"
      style="--delay:240ms; --grad:linear-gradient(120deg, rgba(14,165,233,.55), rgba(14,165,233,.12), rgba(14,165,233,.55));"
    >
      <div class="pointer-events-none absolute inset-0 rounded-2xl opacity-0 transition-opacity duration-300 group-hover:opacity-100"
           style="mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
                  -webkit-mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
                  -webkit-mask-composite: xor; mask-composite: exclude; padding:1px; background:var(--grad);"></div>
      <div class="pointer-events-none absolute inset-0 opacity-0 transition-opacity duration-300 group-hover:opacity-100"
           style="background: radial-gradient(380px circle at var(--x,50%) var(--y,50%), rgba(14,165,233,.08), transparent 40%);"></div>

      <h3 class="relative z-10 font-semibold">Délais & engagements</h3>
      <ul class="relative z-10 mt-3 space-y-2 text-sm text-gray-700">
        <li class="flex items-start gap-2">
          <span class="mt-1 inline-flex h-5 w-5 items-center justify-center rounded-full bg-sky-100 text-sky-700 ring-1 ring-sky-200">
            <!-- clock -->
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              class="h-3.5 w-3.5"
              aria-hidden="true"
              focusable="false"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <circle cx="12" cy="12" r="8" />
              <path d="M12 8v4l3 2" />
            </svg>
          </span>
          <span>Intervention <strong class="text-emerald-600">en moins de 2h</strong> (selon disponibilité).</span>
        </li>
        <li class="flex items-start gap-2">
          <span class="mt-1 inline-flex h-5 w-5 items-center justify-center rounded-full bg-sky-100 text-sky-700 ring-1 ring-sky-200">
            <!-- bolt -->
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              class="h-3.5 w-3.5"
              aria-hidden="true"
              focusable="false"
              fill="currentColor"
            >
              <path d="M13 2L3 14h7l-1 8 10-12h-7l1-8z"/>
            </svg>
          </span>
          <span>Enlèvement sur place en moins de <strong>20 min</strong>.</span>
        </li>
        <li class="flex items-start gap-2">
          <span class="mt-1 inline-flex h-5 w-5 items-center justify-center rounded-full bg-sky-100 text-sky-700 ring-1 ring-sky-200">
            <!-- leaf -->
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              class="h-3.5 w-3.5"
              aria-hidden="true"
              focusable="false"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path d="M4 14c8 0 8-8 16-8 0 8-8 16-16 16 0-3 0-8 0-8z" />
            </svg>
          </span>
          <span>Ponctualité, respect de l’environnement, suivi administratif.</span>
        </li>
        <li class="flex items-start gap-2">
          <span class="mt-1 inline-flex h-5 w-5 items-center justify-center rounded-full bg-sky-100 text-sky-700 ring-1 ring-sky-200">
            <!-- doc -->
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              class="h-3.5 w-3.5"
              aria-hidden="true"
              focusable="false"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path d="M7 4h7l4 4v12H7z" />
              <path d="M14 4v4h4" />
            </svg>
          </span>
          <span>Certificat sous ~1 mois.</span>
        </li>
      </ul>
    </article>
  </div>
</section>

<style is:global>
/* reveal au scroll + délai via --delay */
.reveal { opacity: 0; transform: translateY(8px); transition: opacity .45s ease, transform .45s ease; transition-delay: var(--delay, 0ms); }
.reveal.is-inview { opacity: 1; transform: translateY(0); }
.price-card { cursor: pointer; }

@media (prefers-reduced-motion: reduce) {
  .reveal { opacity: 1; transform: none; transition: none; }
  .price-card { transition: none !important; }
}
</style>

<script>
  // Reveal au scroll
  const io = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        e.target.classList.add('is-inview');
        io.unobserve(e.target);
      }
    });
  }, { threshold: 0.18 });
  document.querySelectorAll('.reveal').forEach(el => io.observe(el));

  // Spotlight sur les cartes
  document.querySelectorAll('.price-card').forEach((card) => {
    card.addEventListener('mousemove', (e) => {
      const r = card.getBoundingClientRect();
      const x = ((e.clientX - r.left) / r.width) * 100;
      const y = ((e.clientY - r.top) / r.height) * 100;
      card.style.setProperty('--x', x + '%');
      card.style.setProperty('--y', y + '%');
    });
    card.addEventListener('mouseleave', () => {
      card.style.removeProperty('--x'); card.style.removeProperty('--y');
    });
  });
</script>
